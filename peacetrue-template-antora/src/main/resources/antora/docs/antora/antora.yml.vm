name: ${repository.name}
title: ${repository.title}
version: master
asciidoc:
  attributes:
    numbered: '@'
    website: ${repository.website}
    app-group: ${repository.group}
    app-name: ${repository.name}
start_page: ROOT:index.adoc
nav:
  - modules/ROOT/nav.adoc
