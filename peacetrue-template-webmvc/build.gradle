plugins {
    id "com.github.peacetrue.gradle.build-convention" version "${peaceGradleVersion}${tailSnapshot}"
}

group "com.github.peacetrue.template"
version "${peaceTemplateSpringbootVersion}${tailSnapshot}"
description "Spring Boot 目录模板"
